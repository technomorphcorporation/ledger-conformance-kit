// THE contract. Clients compile their adapter against this artifact and nothing else.
// Zero dependencies, Java 17 bytecode. Changing anything public is a MAJOR release.
dependencies { /* intentionally empty — do not add anything */ }
